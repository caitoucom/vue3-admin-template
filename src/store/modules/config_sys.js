const config_sys = {
    state: {
        isCollapse: false,
    },
    mutations: {
        updateIsCollapse(state, msg) {
            state.isCollapse = msg
        }
    },
    actions: {},
    getters: {}
}

export default config_sys